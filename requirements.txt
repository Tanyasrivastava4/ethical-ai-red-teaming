# Core ML libs (small defaults)
torch
transformers
accelerate

# Safety & utils
detoxify
transformers[sentencepiece]
pandas
tqdm
pyyaml
openpyxl

# Optional (if using larger models/8bit)
bitsandbytes
safetensors

#run code or pipeline by running these
#pip install -r requirements.txt

#export HUGGINGFACE_HUB_TOKEN="hf_xxx"

#python run_pipeline.py


